apiVersion: v2
name: k2a-token-sync
description: Keeps ArgoCD's downstream cluster registrations valid with short-lived ServiceAccount tokens
type: application
# The version of the chart, not of the application. Bump it when the chart
# changes — a new resource, a renamed or removed value, a changed default — not
# when a new k2a-token-sync release is published.
#
# There is deliberately no appVersion: the application version is image.tag,
# supplied in deployment values. See the image helper in templates/_helpers.tpl.
version: 0.2.0
home: https://github.com/krisiasty/k2a-token-sync
sources:
  - https://github.com/krisiasty/k2a-token-sync
keywords:
  - argocd
  - kubernetes
  - serviceaccount
  - tokens
  - gitops
